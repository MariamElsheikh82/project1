on:
  push:
    branches:
      - master
      
    
    
    
    
jobs:
  build:
    runs-on: ubuntu-latest
        
    steps: 
      - uses: actions/checkout@v3
      - name: "Login to Docker hub"
        run: docker login --username mariamelsheikh82 --password dckr_pat_fi9WlOiAAI3VWBX4s9fd5q2BRJE
    
